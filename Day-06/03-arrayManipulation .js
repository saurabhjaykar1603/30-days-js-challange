// Task 3: Use the push method to add a new number to the end of the array and log the updated array.
let numbers = [1, 2, 3];
console.log(`array before push ${numbers}`);
numbers.push(4);
console.log(`array after push ${numbers}`);

// Task 4: Use the pop method to remove the last element from the array and log the updated array.
let ages = [18, 22, 24];
console.log(`array before pop ${ages}`);
ages.pop();
console.log(`array after pop ${ages}`);

// Task 5: Use the shift method to remove the first element from the array and log the updated arraу.
let teas = ["Masala Tea", "Ginger Tea", "Ice Tea"];
console.log(`array before shift ${teas}`);
teas.shift();
console.log(`array after shift ${teas}`);

// Task 6: Use the unshift method to add a new number to the beginning of the array and log the updated array.
let fruits = ["Apple", "Banana", "Orange"];
console.log(`array before unshift ${fruits}`);
fruits.unshift("Mango");
console.log(`array after unshift ${fruits}`);
